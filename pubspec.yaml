name: simple_barrel_generator
description: A simple barrel file generator for Dart/Flutter projects.
version: 0.0.1
homepage: https://github.com/r-koubou/dart_simple_barrel_generator

platforms:
  windows:
  macos:
  linux:

executables:
  simple_barrel_generator:

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  yaml: ^3.1.3
  path: ^1.9.1


dev_dependencies:
  test: ^1.26.3
  lints: ^6.0.0
